import React from 'react';

/**
 * This component is used to render simple plugins, which don't need in configuration files changes
 * In Core it returns null
 *
 * @return {null|XML}
 */
const ExtraPlugins = () => {
    return null;
};

export default ExtraPlugins;
